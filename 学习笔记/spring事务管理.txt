Spring的事务管理
    spring4之service层事务控制: https://www.jianshu.com/p/f79b93490edd
    spring service层事务管理小结: https://blog.csdn.net/wutongyuWxc/article/details/80622562


1.数据库是否默认自动提交事务
    默认自动提交事务: mySql
    默认不会自动提交: oracle, sybase

2.怎么关闭自动提交事务?
    @Transactional(propagation=Propagation.NOT_SUPPORTED)


一.怎么配置spring的事务管理?
    1.创建事务管理器的bean
        <bean id="transactionManager"  class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
            <property name="dataSource" ref="dataSource"></property>  <!--设置数据源-->
        </bean>
    2.配置事务管理器
        2.1 通过AOP管理事务:
            配置通知和切入点,在此省略
        2.2 或者基于注解管理事务
            <tx:annotation-driven transaction-manager="transactionManager" />
            这样可以用在方法上添加注解的方法管理事务

二.AOP的实现基于java的动态代理
    java动态代理是代理一个类.给这个被代理的类的方法添加增强
