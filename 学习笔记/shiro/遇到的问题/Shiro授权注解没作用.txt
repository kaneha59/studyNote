     不知道为啥,待解决......
     2020.5.18 
          已解决: idea2019不支持java14导致.升级idea 2020后就没问题了

     暂时先使用编码式的授权验证方法吧....
          即, 在Controller方法内部使用subject对象进行授权验证

     如下:
          Subject subject = SecurityUtils.getSubject();
          boolean noAnno = subject.isPermitted("noAnno");
          if()...
          else...